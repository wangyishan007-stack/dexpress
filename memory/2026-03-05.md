
## 2026-03-05 下午/晚上 — 前端测试与 Bug 修复

### 项目
- 工作目录：`/Users/wangxing/xxxc/packages/frontend`
- 项目：Base 链 DEX screener（类 DexScreener），数据来源 GeckoTerminal API

### 关键配置
- exec 安全级别已改为 `full`，不再弹审批窗口
- 群聊 ID：`-5139160470`（tester + web3_coder_bot + wang xing 共同在群里）
- **工作流约定：代码修改任务必须通过群里 @web3_coder_bot，不能自己派 subagent**

### 已修复的 Bug（通过本地 subagent，wang xing 确认保留）
1. `PairList/index.tsx` - `onLoadMore` 移进 `useEffect`
2. `usePairs.ts` - Gainers/Losers 过滤动态跟随时间窗口
3. `useMockPairs.ts` - textFilters（labels/addressSuffixes）实现实际过滤
4. `FilterBar/RankByDropdown.tsx` - 排序选项动态跟随 `dataWindow`

### 最新 commit 新增功能（8528d81）
- `loadMoreSwaps` 实现了（传 `beforeTimestamp` 翻页）
- ETH 价格有 fallback 计算
- Moralis Top Traders / Holders（API key 已配置）
- Uniswap Subgraph LP Providers（Graph API key 已配置）
- TrendingTicker 滚动榜
- gainers/new-pairs 独立页面
- SearchModal / AddPairModal

### 还未修复的 Bug（待 @web3_coder_bot 处理）
1. 🟡 Watchlist 页面 filter 无效（FilterBar tab 切换不过滤列表）
2. 🟡 `dataProvider.ts` filterPools 死代码 change_24h 硬编码
3. 🔴 `NEXT_PUBLIC_MORALIS_API_KEY` 和 `NEXT_PUBLIC_GRAPH_API_KEY` 暴露在浏览器（应去掉 NEXT_PUBLIC_ 前缀，服务端代理）

### API 情况
- GeckoTerminal：有 429 限速风险，本地有 proxy（7890）+ 批量限速保护
- stale cache 降级机制工作正常
- tsc --noEmit 零错误

### wang xing 偏好
- 全程中文对话
- 不喜欢 tester 自己派 subagent 写代码，必须走 web3_coder_bot

---

## 23:00 更新

### 前后端数据全量测试结果（全部通过）
- 主页 Trending / Top Volume / New Pairs：✅
- 详情页 Pool 数据、Trades、Token Info、Other Pools：✅
- Search 搜索：✅
- Moralis Top Traders / Holders：✅
- Uniswap Subgraph LP（仅 Uni V3）：✅
- GoPlus Security：✅
- `/api/pairs` 路由：死代码，没有前端调用（Pair not found 不影响用户）

### 当前剩余 3 个未解决问题
1. ⚠️ tsc 类型错误：`FilterMode` 缺少 `'losers'`，已发给 coder，等修
2. ⚠️ Log in 登录不了：Privy Dashboard 未加 localhost:3000 白名单，需 wang xing 自己去 dashboard.privy.io 操作
3. ⚠️ API Key 安全：NEXT_PUBLIC_MORALIS_API_KEY / NEXT_PUBLIC_GRAPH_API_KEY 暴露浏览器，已发给 coder

### 结论
功能、数据、交互全部正常，无数据错误问题。剩余均为配置/安全/类型问题。
