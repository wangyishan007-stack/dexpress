# 2026-03-10 工作日志

## 项目：base-dex-screener (dex.express)

### 全面前端审查与修复

今天进行了多轮全面的前端代码审查，发现并修复了大量 bug。

#### 修复的 Bug 列表（按提交顺序）

**commit 65c876f** - 第一轮修复
- WebSocket 卸载后持续重连（destroyed ref 标志）
- WebSocket send() 前未检查 readyState
- PairDetailClient timersRef 改 Set 防内存泄漏
- TransactionsTable 过滤后空状态显示错误信息
- useMockPairs hasMore 逻辑修正（||result.hasMore）
- dexscreener.ts buildSparkline 用了未定义的 price 变量
- formatters.ts fmtAge 负数时间保护

**commit 5caa55b** - loadMoreSwaps 修复
- loadMoreSwaps 里 return 跳过 setLoadingMore(false)，改为 finally 块

**commit 7bec31e** - 第二轮修复
- SearchModal/OtherPairsModal base/quote 判断（QUOTE_ADDRS check）
- pairsFetcher 排序前 copy 数组，不污染缓存
- AddPairModal 打开时重新读缓存
- FiltersModal 补充缺失的 buys_1h 过滤器

**commit fc79e4c** - 第三轮修复
- page.tsx handleDataWindow：切时间窗口时同步 sort 字段后缀
- TrendingTicker 改用 SWR 订阅，随主列表刷新（之前空依赖永不更新）
- TradingViewChart MA 图例移入 relative chart div（之前可能错位）
- SkeletonRow 接受 columnConfig，列数和实际一致

**commit d313af9** - 第四轮修复
- PairList 行 hover 联动（加 group/group-hover，之前只亮一半）
- Toast 动画改 CSS transition（之前用了未安装的 tailwindcss-animate）
- Toast 颜色改用项目 token（green/red 而非 green-600/red-600）
- RefreshButton 颜色 token 修正（surface-hover/text-secondary 不存在）
- Sidebar /pair/... 页面高亮 All Coins

**commit e566e9e** - Bubblemaps 修复
- URL 从 app.bubblemaps.io 改为 iframe.bubblemaps.io/map
- 加 partnerId=demo（文档规定，demo 只能 localhost，正式需申请）
- 说明：demo partnerId 在生产环境不可用，需联系 contact@bubblemaps.io 申请

**commit d3d967b** - i18n 多语言补全
- 新增 20 个翻译 key：filter.buys/sells/liquidity/mcap/createdAt, chart.*, bubblemaps.*, common.refresh
- FilterBar rankLabel、RankByDropdown 选项、RefreshButton、BubblePlaceholder、TradingViewChart 全部接入 t()
- 5 种语言（en/zh/ja/ko/id）全部对齐，各 269 个 key

### 技术决策记录

- **StatsBar 时间窗口**：决定永远显示 24H，不随用户选择的时间窗口变化
- **Bubblemaps**：暂时用 demo partnerId，待申请正式 ID 后设环境变量 NEXT_PUBLIC_BUBBLEMAPS_PARTNER_ID
- **多语言**：框架是 next-intl，cookie 存 NEXT_LOCALE，支持 en/zh/ja/ko/id

### 当前状态
- 网站已上线：https://dex.express（以及 https://base-dex-screener.vercel.app）
- Bubblemaps tab 功能受限（需正式 partnerId）
- 所有已知 bug 修复完毕，TS 编译零报错
